- name: Device Information
  hosts: routers
  gather_facts: no

  tasks:

    - name: Show device version
      cisco.ios.ios_command:
        commands:
          - show version
      register: output

    - name: Display output
      debug:
	var:output.stdout[0]
